const random=Math.random() * 10;

console.log(random);

const floorValue=Math.floor(random);
console.log(floorValue);

const ceilValue=Math.ceil(random);
console.log(ceilValue);
console.log(Math.sqrt(ceilValue));

let person={name:"lavkush", age: 25 }

let {age}=person;
console.log(age);
